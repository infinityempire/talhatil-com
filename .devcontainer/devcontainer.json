{
    "name": "TalHatil Autonomous Environment",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-azuretools.vscode-docker"
            ]
        }
    },
    "postCreateCommand": "pip3 install -r delta-agent-core/OpenManus-Setup/requirements-dev.txt || true && playwright install-deps && playwright install chromium",
    "remoteUser": "root"
}
